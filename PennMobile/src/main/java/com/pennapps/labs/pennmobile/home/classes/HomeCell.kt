package com.pennapps.labs.pennmobile.home.classes

/**
 * HomeCell is used in HomeAdapter and HomepageViewModel as the superclass that
 * every cell inherits from
 */
open class HomeCell {
    var type: String = "none"
}
